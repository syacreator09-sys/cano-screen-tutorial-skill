{
  "name": "@cano/screen-tutorial-skill",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Reproducible browser tutorial capture for Claude Code and Codex.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs",
    "audit:release": "node scripts/audit-release.mjs",
    "verify": "npm test && npm run check && npm run audit:release",
    "doctor": "node bin/cano-screen.js doctor"
  },
  "bin": {
    "cano-screen": "bin/cano-screen.js"
  },
  "optionalDependencies": {
    "playwright": "^1.55.0"
  }
}
