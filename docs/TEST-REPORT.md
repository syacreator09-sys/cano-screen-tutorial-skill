# Test report — v0.2

Verification reconstructed and executed locally from the branch implementation:

```text
6 tests
6 passed
0 failed
```

Covered:

- configuration defaults and validation;
- deterministic capture planning;
- new `type`, `select` and `pause` actions;
- sensitive-input blocking;
- domain allow-list enforcement.

Live website execution still requires an authorized local session and platform-specific smoke test on the operator's Mac or Windows computer.
